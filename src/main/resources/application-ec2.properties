# EC2 Environment Configuration
fabric.network.base-path=/opt/empverify/fabric-samples/test-network

# Other settings remain the same as application.properties
# Server configuration for production
server.port=8080

# Security - use environment variables in production
empverify.security.api-keys=${API_KEYS:prod-key-67890,admin-key-54321}